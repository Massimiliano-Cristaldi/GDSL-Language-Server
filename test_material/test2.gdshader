shader_type canvas_item;

int foo(int num) {
	return num + 1;
}

void fragment() {
	const float radius = 5;
	float fade_factor = 1.0;
	float pippo = return return;
	float ginevra;
	
	// float alpha = sqrt(foo(1) + foo(2) + 0.1);
	float alpha = sqrt(1.0 + 1.0 + 0.1);
	COLOR.a = alpha + fade_factor;
}
